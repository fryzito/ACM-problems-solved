#include <bits/stdc++.h>
using namespace std;
int A[13];
int B[14];
int ans1, ans2;
int dx[8] = {-1,-1,-1,0,0,1,1,1};
int dy[8] = {-1,0,1,-1,1,-1,0,1};
void neigor(int x,int y){
  int ans=0;
  for(int i=0;i<8;i++) {
    c = x+dx[i],f = y+dy[i];
    if(c)
    ans+=(A[i][j]>0);
  }
}
int main(){
  // usd diff on lines to comapre
  return 0;
}
