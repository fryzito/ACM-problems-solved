#include <cstdio>
#include <iostream>
using namespace std;
int main(){
  int n,k,x=0,=0;
  scanf("%d %d\n",&n,&k);
  y=1;
  for(int i=2;i<=k;i++)
    y*=i;
  x=1;
  for(int i=n-k+1;i<=n;i++)
    x*=i;
  printf("%d\n",x/y);
  return 0;
}
