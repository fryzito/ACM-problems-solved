#include <cstdio>
#include <iostream>
#include <algorithm>



int main()
{
	 std::cout<< "formato pueva"; // si es necesario formato

	 cin >> a >> ;
     char a = '1';
	 
     std::cout << "Es mayuscula? " << isupper(a) ;

     std::cout << "Es minuscula? " << islower(a) ;

     std::cout << "Es digito? " << isdigit(a) ;

     std::cout << "Es letra? " << isalpha(a) ; 

     std::cout << "Convertir a mayuscula " << (char)toupper(a) ;

     std::cout << "Convertir a minuscula " << (char)tolower(a);
	 
	 return 0;
}
